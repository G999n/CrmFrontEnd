export interface Customer {
    customerId: number;          // Maps to CustomerId
    name: string;                // Maps to Name
    company: string;             // Maps to Company
    industry: string;            // Maps to Industry
    contactDetails: string;      // Maps to ContactDetails
    accountStatus: string;       // Maps to AccountStatus
    lastContactDate: Date;       // Maps to LastContactDate
  }
  